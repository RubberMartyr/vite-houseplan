import * as THREE from 'three';
export declare function buildOpeningGlassGeometry(openingWidth: number, openingHeight: number, frameThickness: number, glassThickness: number, glassInset: number, frameDepth: number): THREE.BufferGeometry;
