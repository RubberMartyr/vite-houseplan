import type { SideWindowSpec } from '../builders/windowFactory';

export type FacadeWindowPlacement = {
  spec: SideWindowSpec;
  xOuterPlane: number;
  zCenter: number;
  width: number;
  height: number;
};
